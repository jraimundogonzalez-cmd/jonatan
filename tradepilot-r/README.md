# TradePilot R

**Every R Matters.**

Software premium de gestión de posiciones para traders discrecionales (Forex, índices, materias primas, acciones, cripto). Agnóstico de estrategia y agnóstico de ratio riesgo/beneficio: todo el producto se basa en R como unidad decimal libre, nunca en ratios fijos.

> TradingView sirve para analizar. TradePilot sirve para gestionar.

Este directorio contiene el **blueprint completo** del producto, diseñado antes de escribir una sola línea de código de la aplicación, siguiendo el orden: Blueprint → UX → Base de datos → Arquitectura → MVP → IA → Escalabilidad.

## Índice del blueprint

| Doc | Contenido |
|---|---|
| [00 · Resumen ejecutivo](./docs/00-resumen-ejecutivo.md) | Visión, problema, propuesta de valor, filosofía del producto |
| [01 · Blueprint de producto](./docs/01-blueprint-producto.md) | Personas, principios, decisiones donde se mejora el brief original (con justificación), alcance negativo |
| [02 · Modelo matemático](./docs/02-modelo-matematico.md) | Fórmulas de R, parciales, esperanza matemática, algoritmo del optimizador, aprendizaje bayesiano personalizado |
| [03 · UX/UI](./docs/03-ux-ui.md) | Arquitectura de la información, flujo de registro <30s, sistema de diseño (tokens, componentes) |
| [04 · Base de datos](./docs/04-base-de-datos.md) | Esquema PostgreSQL/Supabase completo, RLS multi-tenant, DDL |
| [05 · Arquitectura](./docs/05-arquitectura.md) | Stack técnico, motor de cálculo client-side, escalabilidad, seguridad |
| [06 · Sistema de IA](./docs/06-sistema-ia.md) | Optimizador determinista vs. LLM para explicación/visión, roadmap de IA |
| [07 · MVP y roadmap](./docs/07-mvp-roadmap.md) | Matriz RICE, fases V1-V4, métricas de instrumentación |
| [08 · Modelo de negocio](./docs/08-modelo-negocio.md) | Pricing, unit economics, competencia, growth loops |
| [09 · Product Blueprint](./docs/09-product-blueprint.md) | Documento canónico: visión, misión, filosofía, problemas, público, competidores, casos de uso, roadmap, MVP, versiones futuras |
| [10 · UX de registro rápido](./docs/10-ux-registro-rapido.md) | Flujo de registro ≤3 pulsaciones / una mano / <30s: mapa de pulsaciones, zona de pulgar, auditoría de botones, vías más rápidas |
| [11 · Arquitectura a escala](./docs/11-arquitectura-a-escala.md) | Visión a 10 años (Principal Architect): jerarquía de dominio, frontend, backend/API, Supabase, BD a escala, costes, seguridad, autenticación, permisos, logs, backups |
| [12 · Demostración matemática](./docs/12-demostracion-matematica.md) | El "cerebro": R final, €, beneficio sacrificado/conservado, esperanza matemática, impacto de cada parcial y prueba numérica de dominancia del optimizador — solo datos |
| [13 · IA de aprendizaje continuo](./docs/13-ia-aprendizaje-continuo.md) | Aprendizaje 100% personal y continuo: cold start honesto, decaimiento temporal, pipeline de actualización incremental, guardarraíles de que la IA nunca sustituye al trader |
| [14 · Pantallas, wireframes y navegación](./docs/14-pantallas-wireframes.md) | Inventario completo de pantallas con wireframes (móvil) y reglas de transformación a tablet/desktop, flujos principales y mapa de navegación |
| [15 · Base de datos completa](./docs/15-base-de-datos-completa.md) | Normalización (1FN/2FN/3FN) explicada tabla a tabla, versionado (esquema/capital/IA), logs de auditoría append-only, configuración y feature flags — extiende 04 |
| [16 · Modelo SaaS (fundador)](./docs/16-modelo-saas-fundador.md) | Tiers Gratis/PRO/Elite/Team, costes y margen, canal B2B2C con prop firms, roadmap de negocio, retención basada en valor (sin dark patterns) y la IA contada como historia de negocio |
| [17 · TradePilot OS](./docs/17-tradepilot-os.md) | Visión de plataforma: 6 de 9 módulos ya existen (mapeados a los bounded contexts de 11), los 4 nuevos (Replay, Psychology, Tax Report, TradeVault) acotados con el filtro de producto, y la reorientación de TradePilot R a cuenta como centro de gravedad |
| [18 · Cuentas completas y Dashboard Maestro](./docs/18-cuentas-completas-dashboard-maestro.md) | Estado (Challenge/Funded/Live/Pausada/Terminada), drawdown estático vs. trailing, Profit Split separado del modelo matemático puro, y el Dashboard Maestro con prioridad "cuentas en riesgo" |
| [19 · Metodología y reglas del proyecto](./docs/19-metodologia-y-reglas-del-proyecto.md) | Capítulo de proceso: Challenge Mode, formato obligatorio de decisiones, Auditoría del Capítulo (Apple/Linear/TradingView/hedge fund + puntuación 0-100), principio anti-hardcoding (Rule Engine), pie de capítulo obligatorio, principio rector "cada clic debe generar valor" |
| [20 · Flujo funcional del usuario](./docs/20-flujo-funcional-usuario.md) | Recorrido completo desde la instalación hasta cientos de operaciones y consulta de estadísticas — sin pantallas, solo funcional. Auditado (92/100), madurez 95% — **aprobado** |
| [21 · Core vs. Módulos y Plan de Gestión](./docs/21-arquitectura-core-vs-modulos.md) | Revisión arquitectónica bajo Challenge Mode: CORE oficial (8 conceptos), el Plan de Gestión como unidad reutilizable de la que una Operación es una ejecución, Rule Engine como "core por dependencia, módulo por construcción". **Aprobado** |
| [21.5 · Domain Model (DDD)](./docs/21.5-domain-model-ddd.md) | Modelo de dominio profesional: Entities/Value Objects/Aggregates/Domain Services/Domain Events/Bounded Contexts. Hallazgo principal: patrón Snapshot — Plan y Perfil de Reglas no pueden referenciarse en vivo desde Operación/Cuenta sin corromper el histórico matemático. Puntuación 95/100, madurez 95%, dominio validado para 10 años. **Aprobado**, patrón Snapshot elevado a regla permanente (19 regla 13) |
| [22 · Rule Engine (diseño conceptual)](./docs/22-rule-engine.md) | Motor declarativo Library→Definition→Instance→Evaluation. Responde la pregunta Stripe/Shopify/Linear/Notion, descarta explícitamente un motor de expresiones genérico, distingue reglas de estado de cuenta vs. de evento de operación, resuelve dependencias entre reglas (Consistency Rule) y cataloga las 15 reglas pedidas en 7 arquetipos + 1 categoría de cálculo. Puntuación 93/100, madurez 90%. **Aprobado** |
| [22.5 · System Contracts](./docs/22.5-system-contracts.md) | 13 módulos oficiales como cajas negras, cada uno con contrato de 8 campos (responsabilidad/entradas/salidas/lo que nunca debe conocer/eventos/invariantes/errores). Hallazgo principal: Rule Engine y Risk Engine se separan explícitamente (calcula vs. juzga) para evitar responsabilidad duplicada. Dependencias permitidas y prohibidas trazadas, 5 principios SOLID/DDD demostrados con ejemplos concretos de esta misma arquitectura. Puntuación 94/100, madurez 92%. **Aprobado**, "Calcular ≠ Juzgar" elevado a regla permanente (19 regla 14) |
| [23 · Invariantes y Principios (Constitución técnica)](./docs/23-invariantes-y-principios.md) | 15 invariantes y 11 principios de ingeniería, analizados críticamente — 5 ejemplos del fundador corregidos de redacción (incl. uno que contradecía la propia edición auditada de operaciones ya aprobada en 20) y el orden de los principios corregido por contradecir la prioridad #1 ya aprobada (precisión matemática, 19 §6). Cada punto con justificación, riesgo de incumplimiento y caso de excepción razonable (o "ninguno encontrado"). Puntuación 96/100, la más alta del blueprint. **Aprobada como Constitución Técnica permanente** |
| [24 · Ventaja Competitiva](./docs/24-ventaja-competitiva.md) | Estrategia pura, cero tecnología. Honesto sobre los límites reales (TradePilot no tiene efecto de red clásico, el arranque en frío es una vulnerabilidad real frente a un competidor con estadísticas agregadas). El verdadero foso: calibración bayesiana personal acumulada con el tiempo, no replicable con financiación. Amenaza más creíble identificada: una prop firm grande construyendo una versión interna gratuita, no otro startup. 10 ventajas, 10 riesgos, 10 activos estratégicos, ADN permanente. Puntuación 91/100. **Aprobado — Fase 0 (Blueprint) oficialmente finalizada** |

### Fase 1 — Diseño Técnico

| Doc | Contenido |
|---|---|
| [25 · Arquitectura Técnica de Plataforma](./docs/25-arquitectura-tecnica-plataforma.md) | Diseño end-to-end antes de módulo por módulo. Hallazgo principal: los 13 módulos de 22.5 no son 13 microservicios — se organizan en Core Service (monolito modular síncrono), 6 Async Workers, 2 librerías compartidas y mecanismos a nivel de trigger de BD. Responde las 13 preguntas del CTO, clasifica criticidad (incl. un caso "pequeño pero mission critical"), riesgos, cuellos de botella y estrategia de escalabilidad. Puntuación 93/100, madurez 90%. **Aprobado** |
| [26 · TradePilot Quant Engine — Núcleo Matemático](./docs/26-quant-engine-nucleo-matematico.md) | Primer componente técnico: la capa de fórmulas puras por debajo de Risk Engine (que pasa a ser su orquestador, no su calculador — corrige 22.5). Catálogo completo con 3 duplicados reales eliminados (RR conseguido/RR ponderada/Resultado final = R_final; Esperanza = Expectancy) y 4 métricas profesionales añadidas (Win Rate propio, Recovery Factor, ratio de consistencia, rachas). Distingue el drawdown descriptivo (curva histórica) del drawdown de cumplimiento (Rule Engine) — la confusión más importante que resolvía el capítulo. Puntuación 97/100, la más alta del blueprint. **Aprobado** — renombrado oficial de "R Engine" a "TradePilot Quant Engine" aplicado en todo el blueprint |
| [27 · Quant Capabilities](./docs/27-quant-capabilities.md) | Catálogo de capacidades (preguntas, no funciones) en 9 categorías. Hallazgo principal: la categoría "Predicción" podía invitar a cruzar el invariante I9 (nunca predecir mercado) — acotada a proyectar el comportamiento estadístico propio, nunca el precio. Añade optimización restringida por drawdown objetivo (distinta de la optimización penalizada existente) y reafirma Calcular≠Juzgar en la categoría Riesgo. Puntuación 96/100, madurez 93%. **Aprobado** — la restricción de Predicción pasa a formar parte permanente de la Constitución Técnica (23, ampliación de I9) |
| [28 · Quant Language](./docs/28-quant-language.md) | Diccionario oficial bilingüe (inglés técnico/español producto) de ~23 términos. Encuentra 7 ambigüedades reales no resueltas antes: "Session" colisiona peligrosamente entre sesión de mercado y de autenticación; "Strategy" se prohíbe por completo (protege I9); "R" colisiona con el coeficiente de correlación si se implementa la futura correlación entre cuentas; Curva de Capital y Curva de Equity no son la misma curva; "Funding Profile" rechazado por proximidad con "Funding Management"; "Expectation" es un cuarto nombre para lo mismo que "Expectancy". Puntuación 97/100. **Aprobado como vocabulario oficial** |
| [29 · Trader Improvement Engine](./docs/29-trader-improvement-engine.md) | El capítulo conceptual más importante: cómo TradePilot mejora objetivamente a un trader en 6 meses. Trader Evolution Loop (Registrar→Comprender→Aprender→Practicar→Validar→Consolidar), la Coaching Card de 6 campos (cada uno anclado a una capacidad ya aprobada de Quant Engine, sin matemática nueva), y un sistema de priorización con 5 condiciones obligatorias + cero notificaciones push como defensa real contra el "profesor pesado". Decide explícitamente que no es un módulo nuevo, es la política de decisión de AI Engine. Puntuación 95/100, madurez 92%. **Aprobado** |
| [30 · Improvement Prioritization Engine](./docs/30-improvement-prioritization-engine.md) | El Improvement Backlog: ratio Beneficio/Coste sobre 8 factores, con el Riesgo de sobrecorrección como filtro de exclusión dura (nunca solo una penalización) que ofrece una versión incremental del cambio en vez de descartarlo. Máximo 1-2 elementos activos, límite global por usuario no por cuenta; un activo nunca se desplaza a mitad de validación. Salvaguarda explícita contra optimización local: cruce obligatorio con el semáforo de riesgo de cuenta (18 §6) antes de activar cualquier candidato que aumente varianza. Declara honestamente que "coste psicológico" es el factor peor fundamentado del modelo. Puntuación 95/100, madurez 93%. **Aprobado — fase conceptual del producto oficialmente cerrada** |
| [31 · TradePilot Product Operating System (TPOS)](./docs/31-tradepilot-product-operating-system.md) | La Constitución de evolución del producto: sustituye formalmente el filtro informal de 5 preguntas usado desde el inicio. 12 preguntas obligatorias, un gate binario (romper un invariante descalifica sin importar la puntuación — mismo patrón ya usado en 30), puntuación 0-100 en 10 dimensiones con pesos derivados de la prioridad ya aprobada en 19 §6 (no arbitrarios), y un sistema de retirada de funcionalidades con re-auditoría retroactiva periódica — justificado por el propio precedente real del proyecto (el Rule Engine invalidando el `account_rules` de columnas fijas). Extrae 5 principios de evolución de Notion/Linear/Stripe/Shopify/Figma, cada uno ya presente en el blueprint sin haber sido nombrado. Puntuación 96/100, madurez 94%. **Aprobado — Fase 0 (Blueprint Conceptual) oficialmente finalizada** |
| [32 · Public Domain Contracts](./docs/32-public-domain-contracts.md) | Congela los contratos públicos entre módulos antes de escribir código, DB o clases. Hallazgo principal: de los 12 "Aggregate Roots" pedidos, solo 6 lo son de verdad — Snapshot es un Value Object con identidad técnica (no de dominio, corrige una contradicción real con 22.5 §2.11), Scenario es un Value Object, Simulation es un proceso sin estado, Rule Evaluation es un registro inmutable append-only, y Coaching Card + Improvement Backlog se unifican en un único Aggregate Root (Improvement Item) con dos vistas — 12 nombres → 11 contratos reales, cada uno con sus 11 campos. Añade el mapa de comunicación completo, 6 eventos nuevos no nombrados antes (`PerfilDeReglasCreado/Editado`, `MejoraCandidataDetectada/Activada/Consolidada/Descartada`). Puntuación 95/100, madurez 93%. **Aprobado — cierra oficialmente la Fase 0 (arquitectura conceptual)** |

## Fase 1 — Engineering Specifications

Especificaciones técnicas listas para construir, no capítulos conceptuales — cada una implementable directamente por un equipo de ingeniería. Formalizan (nunca reabren) las decisiones ya aprobadas en el blueprint de más arriba.

| Spec | Contenido |
|---|---|
| [001 · TradePilot Quant Engine](./specs/001-tradepilot-quant-engine.md) | Especificación técnica completa del componente más importante del sistema: arquitectura interna (8 subcomponentes, grafo de dependencias sin ciclos), interfaces públicas tipadas (envelope `QuantResult<T>`, catálogo de errores tipado, política de versionado SemVer + `formula_version`), motor matemático completo (kernel decimal sobre `BigInt` — nunca `float` —, redondeo bancario, algoritmo de Welford para agregados en streaming), objetivos de rendimiento y complejidad por función, y el catálogo completo de testing (unit/property/edge/stress/regression + golden dataset con gate de CI). Introduce **Explainable Quant**: toda función pública retorna resultado+fórmula+versión+datos+confianza, consumido por AI Engine sin lógica duplicada. Hallazgo crítico de la auditoría: **el espacio de búsqueda del Optimizer que 02 §5.1 estimaba en 10⁴–10⁵ combinaciones es en realidad del orden de 2,3×10¹¹ para n=5 parciales** — siete órdenes de magnitud de error, que habría hecho el Optimizer inconstruible como fuerza bruta; se traslada a la nueva línea de investigación **TradePilot Labs — Adaptive Management Optimization**, explícitamente fuera del MVP. También añade `calcularDesviacionR` como función propia (evita una duplicación de cálculo de varianza entre `calcularRatioConsistencia` y `calcularScore` que 26 no había detectado). Puntuación 96/100, madurez 95%. **Aprobado — congelado como primera especificación técnica** |
| [002 · Operations Engine](./specs/002-operations-engine.md) | Especificación técnica completa del componente de mayor uso del producto: ciclo de vida con 3 estados (Abierta/Cerrada/**Cancelada**, nuevo), tabla completa de transiciones permitidas/prohibidas, validaciones síncronas vs. asíncronas, 5 invariantes, integración explícita con Quant Engine (siempre vía Risk Engine, nunca directa), Rule Engine (siempre asíncrona, nunca bloqueante) y Funding Management/AI Engine. Resuelve los 7 tipos de registro pedidos (entrada, parciales, Break Even/Stop Loss/Take Profit/cierre manual unificados en un campo `closure_reason`, cancelación con y sin reversión de capital, correcciones auditables, importación futura). La garantía de <30s se convierte en regla arquitectónica no negociable: camino crítico estrictamente acotado, cascada reactiva siempre asíncrona, escritura local-first, idempotencia obligatoria. Arquitectura preparada desde hoy para conectores futuros (TradingView/MT4/MT5/cTrader/NinjaTrader) vía Import Adapters y campos `source`/`external_ref`, sin una segunda vía de escritura con reglas distintas a la manual. 3 hallazgos de integridad reales en la auditoría (estado Cancelada inexistente, sin clave de idempotencia, sin campos de importación) con sus ALTER de esquema. Puntuación 95/100, madurez 94%. **Aprobado — congelado como segunda especificación técnica** |
| [003 · Funding Management](./specs/003-funding-management.md) | Especificación técnica completa del dominio más diferenciador del producto: Empresas y Cuentas como los dos Aggregate Roots ya congelados en 32, con su ciclo de vida, ledger de capital y modelo de escala hasta 100+ cuentas sin cambio estructural. Hallazgo central: **corrige una contradicción real abierta desde el capítulo 20 §0** — el esquema `account_rules` (columnas fijas de límites por cuenta, 04/18) violaba directamente la instrucción del fundador de que "Funding Management nunca debe conocer las reglas internas de una prop firm"; se elimina de este dominio y migra al esquema de Rule Engine, dejando en Funding Management solo una referencia opaca (`rule_profile_snapshot_id`) que nunca se interpreta. Corrige también la lectura de 18 §1/§6: "Drawdown restante" y "Objetivo" nunca fueron campos propios de la cuenta, son proyecciones cruzadas compuestas por Analytics a partir de hechos de capital + veredicto de Rule Engine — solo Profit Split sí es un hecho puro que pertenece aquí. Declara explícitamente que **"Equity" no es implementable hoy** (no hay feed de precio en vivo) y que ofrecerlo sería una aproximación inaceptable — se documenta como capacidad futura, no se fabrica un número. Reserva un estado `merged` para fusiones futuras de cuentas, sin construir el mecanismo completo. Puntuación 96/100, madurez 93%. **Aprobado — congelado como tercera especificación técnica; separación Funding Management/Rule Engine elevada a decisión arquitectónica permanente** |
| [004 · Rule Engine](./specs/004-rule-engine.md) | Especificación técnica completa del sistema de cumplimiento normativo: entrega el esquema real de `rule_definitions`/`rule_profiles`/`rule_instances`/`rule_profile_snapshots`/`rule_evaluations` que SPEC-003 dejó como requisito explícito pendiente, sobre la jerarquía de 7 arquetipos + 1 categoría de cálculo ya aprobada en 22. Corrige la propia instrucción del capítulo: Rule Engine nunca invoca a Quant Engine directamente, siempre vía Risk Engine (reafirma 26 §8). Resuelve la resolución de dependencias entre reglas compuestas (Consistency Rule) de forma **automática por convención** en vez de exigir al usuario conectar un grafo manualmente — aplicación directa del nuevo invariante I16 (Zero Friction). **Hallazgo más importante**: una dependencia real de orden de ejecución — dos evaluaciones concurrentes sobre la misma cuenta podían escribir el estado de cumplimiento desordenado; se corrige con un número de secuencia por evento (`triggering_event_sequence`) que hace la lectura inmune a escrituras tardías, sin necesitar bloqueos que penalizarían el rendimiento. Introduce el principio **Evaluar ≠ Actuar** (reglas de tipo "scaling" nunca disparan una acción, solo un veredicto que otro módulo decide interpretar) como candidato a regla permanente. Añade soporte nativo para reglas temporales, experimentales (`mode: shadow`) y versionado de Rule Definition (mismo patrón que `algorithm_version`). Puntuación 97/100 — la más alta de Fase 1 hasta SPEC-005 — madurez 95%. **Aprobado — congelado como cuarta especificación técnica** |
| [005 · TradePilot Optimizer](./specs/005-tradepilot-optimizer.md) | Especificación técnica completa del mayor diferenciador matemático del producto: descubre configuraciones mejores, nunca las calcula (todo cálculo se delega directamente a Quant Engine, confirmado como consumo directo distinto del caso Rule Engine — 26 §2.7). **Responde con matemática, no con arquitectura, al mandato de "destruir la fuerza bruta"**: demuestra que una búsqueda aleatoria acotada da una garantía de cobertura probabilística independiente del tamaño del espacio (K≥ln(ε)/ln(1−q) — ~300 muestras para 95% de confianza de tocar el 1% superior, sea el espacio 10⁵ o 2,3×10¹¹) y la adopta como estrategia de referencia del día 1, sin depender de investigación futura. Arquitectura basada en el patrón *ask/tell* de optimización de caja negra — la misma interfaz de 3 métodos soporta, sin tocar el núcleo, búsqueda heurística, algoritmos evolutivos, optimización bayesiana y Monte Carlo el día que TradePilot Labs los valide; un registro de estrategias aprobadas impide invocar cualquier algoritmo no validado. Resuelve la aparente tensión entre "nunca caja negra" y algoritmos futuros heurísticos: se explica siempre el **resultado** (métricas objetivas de Quant Engine) nunca el **proceso** de búsqueda. Prohíbe explícitamente presupuestos de optimización por tiempo de reloj (rompería la reproducibilidad) y distingue reproducibilidad-por-semilla de invariancia-al-orden-de-exploración para estrategias estocásticas. Aplica I17 (Evaluate≠Execute) por construcción de tipos: ninguna recomendación se aplica sola. Puntuación 97/100, madurez 94%. **Aprobado — congelado como quinta especificación técnica; estrategia de búsqueda probabilística y patrón Ask/Tell elevados a contrato permanente de evolución** |
| [006 · TradePilot Knowledge Engine](./specs/006-knowledge-engine.md) | Especificación técnica completa de la memoria estructurada del trader: nunca calcula, nunca optimiza, nunca interpreta reglas, nunca recomienda — solo descubre, almacena y actualiza conocimiento con evidencia (`QuantResult<T>` + Trade Set, 28 §2), confianza, fechas y estado (activo/debilitado/descartado). **Hallazgo de consolidación**: no es un componente nuevo desde cero — absorbe formalmente tres piezas que ya existían dispersas sin dueño único (13, aprendizaje bayesiano; 27 §2.8, Diagnóstico; 29 §4, re-ejecución continua descrita solo en prosa), añadido como 14º módulo oficial. Cubre las 15 dimensiones pedidas con solo 4 arquetipos de detector — tercera vez que el mismo patrón arquitectónico (catálogo pequeño de primitivas, extensible sin tocar el núcleo) resuelve el problema, tras Rule Engine y Optimizer. **Hallazgo estadístico central**: corrige el problema real de comparaciones múltiples — evaluar docenas de segmentaciones a la vez con el mismo umbral de confianza de una hipótesis aislada produciría "patrones" espurios por puro azar; se aplica control de tasa de falsos descubrimientos antes de promover cualquier hallazgo. El "olvido automático" pedido reutiliza literalmente el decaimiento exponencial de 13 §4 en vez de inventar un mecanismo nuevo, lo que además mitiga gratuitamente el sesgo de *peeking*/optional stopping. Declara honestamente que la confusión/correlación falsa entre dimensiones **no se resuelve** en esta especificación — se documenta como limitación real, no se disfraza. Puntuación 97/100, madurez 94%. **Aprobado — congelado como sexta especificación técnica; separación Knowledge Engine/Analytics Engine elevada a decisión arquitectónica permanente** |
| [007 · TradePilot Analytics Engine](./specs/007-analytics-engine.md) | Especificación técnica completa de la capa oficial de lectura: nunca escribe, nunca calcula reglas, nunca genera conocimiento, nunca optimiza, nunca decide — solo transforma hechos y conocimiento ya producidos en información visual. **Requisito central resuelto por esquema, no por disciplina**: el Catálogo Oficial de KPIs (`kpi_definitions`) obliga a que cada métrica tenga una única fila, trazable a un único módulo y función de origen — estructuralmente imposible tener dos formas de calcular lo mismo. Cuarta aparición confirmada del mismo patrón arquitectónico (catálogo pequeño de primitivas, extensible sin tocar el núcleo) tras Rule Engine, Optimizer y Knowledge Engine. **Hallazgo principal**: de las nueve variantes de visualización pedidas (una cuenta/varias/empresa/comparativa entre empresas/evolución histórica/por sesión/por instrumento/por plan/por etiqueta), ninguna necesita pantalla propia — todas son la misma consulta genérica (`AnalyticsQuery`, reutiliza Trade Set de 28 §2) con distinto `scope`/`group_by`, eliminando por diseño la redundancia que el fundador pidió expresamente buscar. Reafirma la frontera ya establecida con Reporting Engine (22.5 §2.13) para Informes/Exportaciones en vez de disolverla. Rechaza explícitamente dos visualizaciones candidatas por no superar el filtro "¿ayuda a mejorar al trader?" (conteo de victorias/derrotas sin ponderar por R, mapa de actividad sin KPI). Declara honestamente que "evolución por etiquetas" depende de un campo que Operations Engine todavía no tiene — no lo inventa para completar la lista. Auditoría de I16 confirma cualquier dato importante accesible en ≤3 interacciones. Puntuación 96/100, madurez 93%. **Aprobado — congelado como séptima especificación técnica** |
| [008 · Trade Capture Engine](./specs/008-trade-capture-engine.md) | Especificación técnica completa del pilar de fricción cero en captura de datos: nunca analiza, nunca optimiza, nunca interpreta reglas, nunca genera conocimiento — solo reconstruye hechos históricos con máxima fidelidad. **Consolidación**: es exactamente el Import Adapter que SPEC-002 §5.7/§8.5 ya había anticipado y para el que ya había reservado los campos `source`/`external_ref` — no reabre ese contrato, lo construye por completo; añadido como 15º módulo oficial. El registro manual (10) se reconoce como un Connector más entre ocho plataformas nombradas (MT4/5, cTrader, Tradovate, Rithmic, DXTrade, MatchTrader, TradingView) — quinta aparición confirmada del patrón de catálogo pequeño extensible sin tocar el núcleo. Traduce I18 (Automation Before Interaction) a un pipeline literal de tres etapas obligatorias. **Hallazgo matemático central**: Comisiones y Deslizamiento no tenían dónde vivir en el modelo matemático vigente — se resuelven con el mismo precedente ya usado para Profit Split (18 §3): hechos de ejecución que nunca entran en `R_final`, separados en campos propios que solo alimentan la reconciliación real de capital. Declara con precisión estructural, no como limitación técnica temporal, que `rr_objetivo` y sobre todo `be_trigger` son conceptos de planificación que a veces no dejan rastro verificable — cuando no hay certeza, cae a interacción manual en vez de adivinar. Añade una tabla de especificación de unidades por instrumento para prevenir el error más peligroso del documento: una normalización ingenua de pips/puntos/lotes que produciría un `risk_pct` "verosímil pero incorrecto". Desglose honesto del 95%: alcanzable como promedio ponderado, no como garantía campo a campo. Puntuación 97/100, madurez 94%. **Aprobado — congelado como octava especificación técnica** |
| [009 · AI Journal Engine](./specs/009-ai-journal-engine.md) | Especificación técnica completa del cierre conversacional del journal: hallazgo de mayor severidad de toda la Fase 1 hasta ahora — la propuesta original pedía identificar "rangos, liquidez, zonas relevantes" desde una captura, lo que habría violado directamente I9 ("TradePilot nunca evalúa ni opina sobre la calidad de una entrada de mercado", 23) — el propio capítulo 23 había anticipado casi textualmente este escenario como riesgo a vigilar. **Aplicado el gate binario de TPOS explícitamente**: esas tres capacidades se rechazan sin excepción; entrada/stop/TP/parciales (hechos ya decididos por el trader) sí se mantienen, ya cubiertos por Media Engine V3 (22.5 §2.12). Deduplicación exhaustiva de los quince campos pedidos: "Plan utilizado" ya es un hecho (PlanSnapshot, sin necesitar IA), "Calidad de ejecución" es sinónimo de `%_beneficio_conservado` (02 §3, ya existente), "Errores/Aciertos detectados" pertenecen a Knowledge Engine (solo se consultan, nunca se duplican) — de quince campos pedidos, solo 2-3 son genuinamente nuevos (título, resumen, sugerencia de etiquetas, resolviendo el hueco declarado en SPEC-007 §12.1). Corrige el propio ejemplo del fundador: "¿seguiste tu plan?" se pre-rellena por comparación determinista Plan vs. ejecución real (I18 aplicado literalmente) en vez de preguntarse desde cero. Rechaza explícitamente la lectura de "más rápido responder que ignorar" como patrón oscuro — la opción de omitir nunca cuesta más toques que responder. Séptima instancia confirmada del principio "nunca inventar, siempre marcar procedencia y confianza". Añadido como 16º módulo oficial. Puntuación 97/100, madurez 94%. **Aprobado — congelado como novena especificación técnica** |
| [010 · AI Decision Center](./specs/010-ai-decision-center.md) | Especificación técnica completa del árbitro de atención del producto — nunca calcula, nunca descubre, nunca optimiza, nunca hace coaching: decide cuáles de todos los hallazgos posibles merecen una de un máximo absoluto de 5 tarjetas al abrir la app. **Resuelve el problema central sin inventar una fórmula nueva**: comparar "riesgo" contra "mejora" no usa una puntuación única — usa una jerarquía de 7 clases (mismo estándar que cualquier mesa de riesgo profesional/terminal financiero), donde una clase inferior nunca desplaza a una superior, evitando que ruido de baja severidad silencie un riesgo real. Da por fin un hogar al recordatorio pasivo de cuenta que el capítulo 20 dejó pendiente desde el inicio mismo de Fase 0. **Auditoría psicológica explícita, instrucción directa del fundador**: rechaza que "Logros" pueda basarse en frecuencia/volumen de operaciones (incentivaría el sobretrading, lo opuesto a la misión del producto), amplía el riesgo de erosión de autonomía ya identificado en 29 exigiendo que cada tarjeta enlace siempre a la vista completa, y añade la salvaguarda más importante del documento: **el arbitraje nunca puede calibrarse contra señales de engagement** — ninguna cuenta de "toques" puede entrar jamás en qué tarjeta se muestra, la protección estructural exacta contra que el producto derive hacia un feed de atención. Introduce y operacionaliza **I19 — Attention Is the Most Valuable Currency** como invariante permanente, escalando el "coste psicológico" que 30 ya reconocía como su punto más débil a un gate arquitectónico de todo el sistema. Añadido como 17º módulo oficial. Puntuación 97/100, madurez 94%. **Aprobado — congelado como décima especificación técnica** |
| [011 · TradePilot Simulation Engine](./specs/011-simulation-engine.md) | Especificación técnica completa del laboratorio de decisiones de gestión: nunca inventa una operación, entrada o movimiento de precio — siempre reutiliza `r_max` real del propio trader. **Hallazgo central**: de las preguntas "¿qué habría pasado?" pedidas, la mitad (¿y si solo hubiera operado Londres/EURUSD/cuando seguía mi plan?) no son simulaciones en absoluto — son filtros retrospectivos sobre operaciones reales ya cerradas, ya resueltos por Analytics; construir un motor de filtrado propio aquí habría duplicado exactamente lo que SPEC-007 ya tiene. Solo la otra mitad (cambiar parciales/BE/RR/Plan manteniendo el mismo `r_max` real) es un contrafactual genuino, resuelto vía `simularGestion` de Quant Engine. Resuelve una aparente tensión con 32 §3.10 (Simulation como proceso efímero): lo que se guarda/nombra/duplica/comparte nunca es un resultado congelado, es la configuración de entrada — una simulación guardada se recalcula siempre fresca, mejorando con cada operación real nueva. La adopción de una simulación como Plan real reutiliza íntegramente el ciclo de vida de Improvement Item (30/32 §3.7) en vez de inventar un mecanismo de antes/después paralelo, incluido el cruce obligatorio con el semáforo de riesgo de cuenta. Declara con honestidad un límite epistémico permanente (una simulación no puede capturar cómo el propio trader se habría comportado de forma distinta al intentar seguir una regla nueva) y aplica una divulgación — no un bloqueo — contra el riesgo de comparaciones múltiples en exploración manual. Identifica Monte Carlo como más cercano de lo que parece (reutiliza el bootstrap ya existente desde 02 §5.2), a diferencia de Walk Forward/Stress/Portfolio, correctamente diferidos a TradePilot Labs. Añadido como 18º módulo oficial. Puntuación 97/100, madurez 94%. **Aprobado — congelado como undécima especificación técnica** |
| [012 · TradePilot Design System (TPDS)](./specs/012-tradepilot-design-system.md) | Especificación completa del sistema de diseño — no un módulo de dominio (22.5), sino infraestructura compartida como `quant-engine`. **Consolidación, no reinvención**: buena parte de lo pedido ya estaba fijado en 03 §5-8 (paleta, tipografía monoespaciada, StatTile/RChip/ExplainCard, mobile-first, WCAG AA) — se hereda sin cambios, se extiende solo donde faltaba. **Hallazgo más importante**: el mapeo nuevo "Verde = Confirmación" colisiona directamente con "Verde = Beneficio" que 03 §5.2 ya había fijado deliberadamente fuera de ambigüedad — se corrige con un token `--success` distinto de `--positive`, siempre emparejado con icono, nunca solo junto a una cifra de R/€. Reduce el catálogo de 20 componentes pedidos a 17 reales: "Indicador" no es un componente (es la taxonomía que Semáforo/Badge/RChip/Progress ya instancian), "Widget" se rechaza (vía de escape para no clasificar nada correctamente), "Avatar" se difiere (sin caso de uso multi-persona validado, 21.5 §3.1). Identifica que Bloomberg Terminal, una de las referencias pedidas, es el ejemplo menos calmado posible — extrae solo la alineación numérica de precisión, rechaza explícitamente el resto de su estética. Conecta la jerarquía visual pedida (Riesgo>Decisión>Acción>Información>Decoración) como la forma pixel exacta de la jerarquía de 7 clases ya aprobada en SPEC-010, no como una regla nueva independiente. Introduce y operacionaliza **I20 — Professional Calm**, distinguiendo con precisión urgencia artificial (prohibida) de severidad real comunicada con calma (obligatoria) — resuelve explícitamente el caso de mayor riesgo de violarla (la tarjeta de "Logro" de SPEC-010). Señala, en su octava aparición acumulada, que la gobernanza de catálogos de Fase 1 ya no es una nota recurrente sino la deuda de proceso más consistente del proyecto. Puntuación 97/100, madurez 94%. **Aprobado — congelado como duodécima especificación técnica** |
| [013 · TradePilot Experience Architecture (TPXA)](./specs/013-experience-architecture.md) | El puente entre arquitectura técnica, Design System y psicología del trader — cómo debe sentirse una sesión completa, no pantalla a pantalla. **Sucesor explícito del capítulo 20, no un segundo mapa**: actualiza su bucle operativo y su revisión periódica con los ocho motores construidos desde entonces (Trade Capture Engine, AI Journal Engine, AI Decision Center...) en vez de redibujar el recorrido completo desde cero. **Consolida 5 de los 22 recorridos pedidos en 2**: "Primer uso/Onboarding/Crear empresa/cuenta/plan" ya era un solo recorrido continuo (20); "Registro manual/automático" y "Cerrar operación/Completar journal" son, cada par, el mismo momento con distinto grado de automatización (SPEC-008/009); "Cambiar de cuenta" ya estaba resuelto por la arquitectura de información de 03. Introduce y operacionaliza **I21 — One Thought Rule**, y la verifica explícitamente contra la tarjeta ya aprobada de SPEC-009 (dos preguntas simultáneas) para confirmar que no la viola — distingue con precisión decisiones que exigen deliberación de confirmaciones de reacción inmediata, formalizando lo que 03 §4 ya hizo una vez de forma ad-hoc. Exige que "adoptar una simulación como Plan real" (SPEC-011) tenga siempre su propio momento, nunca compartido con otra decisión. Declara explícitamente que eliminar una cuenta o restaurar un backup deben tener fricción deliberada — I16 rige solo lo frecuente, nunca lo irreversible. Puntuación 97/100, madurez 95%. **Aprobado — congelado como decimotercera especificación técnica** |
| [014 · TradePilot Trust Layer](./specs/014-trust-layer.md) | La Constitución de confianza del producto — última especificación de Fase 1. **Hallazgo principal**: de los nueve principios "nunca" y cinco "siempre" pedidos, la enorme mayoría ya estaban construidos, dispersos, en las trece especificaciones anteriores (I18, I20, Explainable Quant, la FDR de Knowledge Engine, el rechazo de gamificación de SPEC-010...) — este documento los audita retroactivamente uno por uno contra las 13 specs, confirma que ninguna los viola, y solo añade las cuatro piezas que faltaban de verdad: una taxonomía de origen visible obligatoria (importado/calculado/detectado/inferido/manual, hasta ahora solo interna), un campo de "supuestos" estructurado en toda recomendación, una regla de completitud de métricas (nunca mostrar un subconjunto favorable de una comparación), y un Protocolo de Divulgación de Errores Propios — anclado en el precedente real ya vivido en este mismo proyecto (el bug de `R_cierre_resto` corregido en el capítulo 02). Añade una regla de lenguaje contra la exageración (distinta de la invención: un dato real presentado con superlativos). **Riesgos legales declarados con honestidad, no disfrazados de solución técnica**: la clasificación regulatoria de las recomendaciones de gestión, y la tensión real entre `audit_log` append-only y un eventual derecho al olvido — ambos marcados como decisiones que requieren asesoría legal, nunca resueltos por arquitectura. Puntuación 98/100 — la más alta de todo el proyecto — madurez 96%. **Aprobado — Fase 1 (Engineering Specifications) oficialmente cerrada: 18 módulos de dominio, 2 piezas de infraestructura compartida, 21 invariantes permanentes (I1-I21)** |

**I16 — Zero Friction** *(invariante permanente, 19)*: TradePilot debe minimizar permanentemente la fricción del usuario. Toda funcionalidad frecuente debe ejecutarse en el menor número posible de interacciones; si una tarea habitual requiere más de 30s o más de 10-12 acciones, debe justificarse con una auditoría de valor o rediseñarse. Se revisa explícitamente en toda especificación desde SPEC-004 en adelante.

**I17 — Evaluate ≠ Execute** *(invariante permanente, 19)*: TradePilot nunca modifica automáticamente el estado crítico de un usuario únicamente porque una regla lo indique. Rule Engine (y, por extensión, cualquier componente que produzca un veredicto o una recomendación — Optimizer incluido, SPEC-005) produce evaluaciones, nunca ejecuta acciones; toda acción pertenece siempre a otro componente responsable, o al propio usuario de forma explícita.

**I18 — Automation Before Interaction** *(invariante permanente, 19)*: TradePilot siempre intenta capturar, interpretar y completar automáticamente toda la información posible antes de solicitar cualquier dato al usuario. Orden obligatorio: 1) captura automática, 2) inferencia segura, 3) interacción manual únicamente cuando sea imprescindible. Primera aplicación completa en SPEC-008 (Trade Capture Engine).

**I19 — Attention Is the Most Valuable Currency** *(invariante permanente, 19)*: ninguna funcionalidad podrá competir por la atención del trader sin demostrar un beneficio esperado superior al coste cognitivo que introduce. Operacionaliza el `Beneficio` de 30 §2.2 frente a un coste cognitivo calibrado sobre el mismo eje que I16 — escala el "coste psicológico" (el factor que 30 ya reconocía como el más débil de su propio modelo) a un gate arquitectónico de todo el sistema. Primera aplicación completa en SPEC-010 (AI Decision Center), con la salvaguarda explícita de que el arbitraje de atención nunca puede calibrarse contra señales de engagement.

**I20 — Professional Calm** *(invariante permanente, 19)*: TradePilot debe reducir el estrés del trader, nunca aumentarlo — ninguna pantalla, gráfico, animación, sonido o interacción puede generar sensación de urgencia artificial. Distingue con precisión urgencia artificial (fabricada por el propio diseño: parpadeo, sonido de alarma, cuenta atrás) de severidad real (un riesgo genuino, comunicado con jerarquía visual estática y lenguaje preciso, nunca ocultado ni suavizado). Primera aplicación completa en SPEC-012 (Design System).

**I21 — One Thought Rule** *(invariante permanente, 19)*: en cualquier pantalla, el usuario solo debe pensar en una cosa importante a la vez — ninguna pantalla puede exigir dos decisiones cognitivas importantes simultáneas. Distingue decisiones que exigen deliberación (protegidas por la regla) de confirmaciones de reacción inmediata (permitidas juntas). Primera aplicación completa en SPEC-013 (Experience Architecture), verificada explícitamente contra la tarjeta de SPEC-009 para confirmar que no la viola.

**TradePilot Labs — Adaptive Management Optimization** *(línea de investigación, fuera del MVP)*: creada tras el hallazgo de SPEC-001 sobre el espacio de búsqueda del Optimizer (≈2,3×10¹¹ combinaciones). Objetivo: encontrar algoritmos capaces de explorar ese espacio de forma eficiente (heurísticas, optimización multiobjetivo, búsqueda bayesiana, algoritmos evolutivos). Mientras no concluya, Quant Engine sigue evaluando una única configuración candidata cada vez (SPEC-001 §3.8).

## Fase 2 — Construcción del MVP

Cambio oficial de metodología: la prioridad deja de ser diseñar y pasa a ser entregar software funcional. Una especificación nueva solo se escribe cuando la implementación real descubra una necesidad que el blueprint no había previsto — nunca por hábito de seguir documentando.

| Doc | Contenido |
|---|---|
| [015 · MVP Implementation Roadmap](./specs/015-mvp-roadmap.md) | El plan de construcción real: grafo de dependencias completo (qué puede empezar cuándo, qué se paraleliza), siete entregas concretas de MVP 0.1 a 1.0, cada una con objetivo/componentes/qué puede y no puede hacer el usuario/riesgos/pruebas de cierre. **Hallazgo previo al propio roadmap**: Fase 1 dejó 5 módulos del mapa original de 22.5 sin especificación de ingeniería propia (Identity, Risk Engine, Management Plans, Audit Engine, Snapshot Engine) — no por omisión, sino porque son integración externa o pura orquestación ya descrita en otras specs; se clasifican aquí en vez de escribir cinco documentos más. Encuentra que Improvement Prioritization Engine completo y Notification Engine serían prematuros para el MVP — todos los casos de uso de notificaciones ya se resolvieron como tarjetas pasivas de AI Decision Center, nunca push. **Simplificaciones deliberadas para evitar sobreingeniería**: el registro manual llama a Operations Engine directamente en el MVP (Trade Capture Engine completo se construye cuando llegue el primer conector automático real, no antes); AI Decision Center arranca con solo 2 de 7 clases de tarjeta; Rule Engine con 4 de 15 tipos de regla; Analytics con 6 KPIs del catálogo completo — ninguna reduce precisión ni fricción, todas recortan solo superficie de catálogo ya preparada para crecer por datos, nunca por código. Señala que el propio objetivo del MVP declarado por el fundador omitía, sin nombrarlos, Rule Engine y Funding Management — indispensables para que exista una Cuenta. Puntuación 97/100, madurez 95%. **Aprobado — Fase 1 cerrada oficialmente, comienza la construcción real del software** |
| [MVP 0.1 — Documento de implementación](./implementation/mvp-0.1.md) | Primer documento de ingeniería ejecutable, sin código: alcance exacto = Quant Engine completo + Identity + Funding Management + libro de capital, antes de que exista ninguna Operación. Cubre las 14 secciones pedidas (objetivo, problema, componentes presentes/ausentes, flujo de usuario paso a paso, arquitectura técnica, DDL completo, API pública, casos de uso, estados/transiciones, testing, rendimiento, riesgos, auditoría Challenge Mode). **Decisiones técnicas resueltas en el propio documento**: kernel decimal = `decimal.js` (soporte nativo de `HALF_EVEN` y precisión configurable en un único punto, `packages/quant-engine/src/decimal/kernel.ts`); el dinero viaja como `string`, nunca `number`, en todo límite de API para blindar contra contaminación de coma flotante. **Hallazgos de Challenge Mode**: una restricción `CHECK` con subconsulta (`personal_account_no_profit_split`) es inválida en Postgres — corregida a un trigger `BEFORE INSERT OR UPDATE` (`validate_account_profit_split()`); construir ya la máquina de estados completa de 6 valores de `accounts.status` habría sido sobreingeniería — el esquema la deja preparada pero solo se implementa lógica de transición para las 2 transiciones accionables por el usuario (Live⇄Paused, Challenge→Funded), dejando `terminated`/`merged` explícitamente para cuando existan Rule Engine y Capa 6. Incluye golden dataset con 4 casos numéricos exactos de R_final y pruebas de aislamiento RLS y de disparador de capital. Puntuación 97/100, madurez 96%. **Aprobado — comienza la implementación en código** |
| **MVP 0.1 — código real** (`packages/quant-engine`, `supabase/`, `apps/web`) | Implementación en dos bloques. **Bloque 1** (Quant Engine Grupo B + esquema/RPC de Funding Management): kernel decimal, `calcularRFinal` y derivadas, migraciones completas con RLS, 5 funciones RPC, API tipada — corrigió un error aritmético del golden dataset aprobado (`1.6600`→`1.7800`), generalizó `cierre_manual_rr` de SPEC-001, y cerró un bypass real de aislamiento multi-tenant vía FK sin RLS. **Bloque 2** (autenticación + onboarding + Dashboard + Nueva Cuenta + Detalle + Registrar Evento + layout, `implementation/mvp-0.1.md` §14.8): magic link de Supabase con middleware de protección de rutas, 4 componentes mínimos del Design System (SPEC-012), y las funciones de lectura (`listar_empresas`/`listar_eventos_capital`) que el catálogo original nunca había expuesto. **Hallazgos reales de esta ronda**: un route group `(onboarding)` generaba `/empresa` en vez de `/onboarding/empresa` (detectado solo al ejecutar `next build` real); `redirect()` llamado dentro de una Server Action invocada imperativamente habría reportado un cierre de sesión exitoso como error; una vulnerabilidad de salto abierto en el `redirectTo` post-login, corregida con un saneador dedicado; una invalidación de caché incompleta que habría dejado el capital del Dashboard desactualizado tras registrar un evento. 68 tests (19 Quant Engine + 49 web), `tsc`/`eslint` limpios, `next build` real verificado (11 rutas). **No verificable en este entorno**: cualquier prueba contra un proyecto Supabase real (RLS de extremo a extremo, magic link real) — no hay credenciales disponibles. |
| **BUILD 002 — Quant Engine completo** (`packages/quant-engine`, Grupos C-F) | Cierra el catálogo matemático completo de SPEC-001 antes de tocar ningún módulo nuevo (prioridad explícita del fundador). Grupo C+G (stats: esperanza/desviación con Welford compartido batch+incremental, ratio de consistencia, profit factor, win rate, recovery factor, racha máxima, tiempo medio en mercado, distribución de R), Grupo D (curvas de equity/drawdown histórico), Grupo E (`calcularDrawdownState`: static/trailing/eod), Grupo F (`simularGestion`/`calcularScore`). **Correcciones aplicadas a la propia SPEC-001** (§8.6, documentadas ahí): `calcularCurvaEquity` generalizada sobre el brand del kernel (`Money|RValue`) — la firma original tipaba `Money[]` incondicionalmente incluso con `unidad="R"`; `calcularTiempoMedioEnMercado` migrada de `number[]` nativo a un 4º brand `Seconds` del kernel decimal — era la única función de todo el catálogo que violaba "nunca float" en su propia firma; `DrawdownStateInput.peak_capital` renombrado a `peak_capital_basis` ("eod" necesita un pico distinto de "trailing", nunca el mismo campo); `max_daily_drawdown_pct` eliminado por ser un parámetro muerto; catálogo de errores ampliado con `ZERO_CAPITAL`. **Duplicación real encontrada y unificada**: `computeRFinal` y `calcularImpactoPorParcial` recalculaban la misma expresión por parcial por separado — unificadas en `decomposeRFinal`. **Hallazgo de rendimiento vía benchmark real** (no teórico): la precisión del kernel decimal estaba en 50 dígitos significativos, casi el doble de los ~28-30 que SPEC-001 §4.3.2 exige — con 50 dígitos, los agregados de Grupo C sobre 10.000 operaciones excedían su objetivo de latencia; reducida a 30, confirmado sin cambiar ningún resultado de los tests ya existentes. El primer benchmark además medía el objetivo equivocado (modo batch en vez de modo streaming, que es literalmente lo que SPEC-001 §5.1 especifica para ese objetivo) — corregido. 126 tests en total (77 en `packages/quant-engine` — Grupo B de BUILD 001 más Grupos C-F de este build — + 49 en `apps/web`), incluyendo property tests, stress tests a 100.000/50.000 elementos, y benchmarks reales con `vitest bench`. `tsc`/`eslint` limpios en todo el monorepo. |
| **BUILD 003 — Risk Engine** (`packages/risk-engine`, `supabase/migrations/20260803120400_risk_engine.sql`, `supabase/tests/`) | Quant Engine declarado congelado para producción (solo se toca ante bug matemático, problema de rendimiento demostrado o violación de invariante) — arranca el primer consumidor oficial: Risk Engine, un orquestador puro (Ports & Adapters) que invoca Quant Engine, persiste el `WelfordAccumulator` por Cuenta, mantiene concurrencia optimista + idempotencia + un outbox transaccional de eventos de dominio, y nunca contiene una fórmula matemática propia — "Calcular ≠ Juzgar" aplicado también al primer consumidor, no solo al futuro Rule Engine. Dominio + puertos + `RiskEngineService` (vía síncrona `calcularResultado`, vía incremental O(1) `procesarOperacionCerrada`, vía de reconstrucción completa `procesarOperacionEditada`/`procesarOperacionCancelada` — Welford no admite resta O(1), SPEC-001 §5.3 autoriza explícitamente reconstruir desde la muestra vigente), adaptadores Supabase (`SupabaseAccountRiskStateRepository`, `SupabaseCapitalFactsProvider` — reutiliza `obtener_cuenta` de Funding Management, nunca duplica su esquema), 24 tests con repositorios en memoria + cliente Supabase con doble de red. **Hallazgo metodológico real**: la primera validación de RLS contra Postgres real corrió como superusuario y "pasó" dos comprobaciones que en realidad eran fallo total de aislamiento — los superusuarios (y el propietario de la tabla) están exentos de RLS sin excepción; corregido conectando como un rol `authenticated` real, lo que reveló un fallo genuino (faltaban políticas de INSERT en dos tablas nuevas, ya que la función atómica es `SECURITY INVOKER`). **Validación de concurrencia real** (no solo simulada en secuencia): dos sesiones de Postgres genuinamente simultáneas compitiendo por el mismo `event_id` sobre la misma Cuenta confirman que el bloqueo de fila serializa correctamente la escritura — una aplica, la otra detecta el conflicto tras el commit de la primera, sin doble conteo ni escritura parcial. 15 comprobaciones SQL de aislamiento/idempotencia/concurrencia contra Postgres real documentadas en `supabase/tests/`. `tsc`/`eslint`/`npm test` limpios en todo el monorepo (77 + 24 + 49 = 150 tests). |
| **BUILD 004 — Operations Engine + Management Plans** (`packages/operations-engine`, `supabase/migrations/20260803120500_operations_engine.sql`, `supabase/tests/03_operations_engine.sql`) | Auditoría previa al código (Challenge Mode, sin escribir una línea todavía) encontró que "Management Plans Engine" + "Smart Trade Assistant", tal como se pidieron, duplicarían motores ya diseñados en Fase 1 (Optimizer, Simulation Engine, Knowledge Engine, AI Decision Center, Trade Capture Engine) — Management Plans es "CRUD simple + Snapshot" (SPEC-015 §2), nunca un motor matemático, y Operations Engine (SPEC-002) no existía en código: sin `trades`, ningún asistente de gestión en curso tiene nada que gestionar. Aprobado por el fundador: construir primero Operations Engine + Management Plans (este build) y dejar congelada, sin construir, la visión de "Gestión sugerida" para BUILD 005 (`implementation/mvp-0.1.md` §14.12) — reutilizando exclusivamente motores existentes, cero fórmulas nuevas, nunca datos de mercado (I9), siempre I17 (Evaluate ≠ Execute). Ciclo de vida completo de la Operación: `management_plans`/`management_plan_partials` (plantilla) + `trades` con el PlanSnapshot embebido (regla 13) — todo lo capital-neutral en SQL puro (`operations.sql`, sin fórmulas, mismo patrón que `funding.sql`), solo `cerrarOperacion`/`editarOperacion` en TypeScript (`packages/operations-engine`, depende de `@tradepilot/risk-engine`, nunca de `@tradepilot/quant-engine` directamente). Dos triggers hacen cumplir invariantes a nivel de Postgres (máquina de estados + inmutabilidad de `account_id`; auditoría automática de toda edición) — inmunes a un bug futuro de la capa de aplicación. El cierre/edición/reversión de capital reutiliza el trigger de recálculo que Funding Management ya tenía (BUILD 001), un `event_type` nuevo, cero fórmulas nuevas. **Hallazgo real de Challenge Mode**: una llamada SQL posicional (no el adaptador TypeScript, inmune por diseño) desplazó silenciosamente sus argumentos tras insertar un parámetro nuevo en mitad de una función — sin ningún error de Postgres, escribiendo datos en columnas equivocadas; corregido moviendo el parámetro al final y reescribiendo todas las llamadas de test a notación con nombre. 15 tests unitarios (servicio con dobles en memoria + adaptador Supabase) + 19 comprobaciones SQL contra Postgres real (máquina de estados, idempotencia, cascada de capital completa, `audit_log`, aislamiento RLS). `tsc`/`eslint`/`npm test` limpios en todo el monorepo (77 + 24 + 15 + 49 = 165 tests). |
| **BUILD 005 — Optimizer** (`packages/optimizer`, corrección documental de `specs/005-tradepilot-optimizer.md`) | Primer motor de **descubrimiento** del producto: busca configuraciones de gestión que el trader no habría encontrado solo, delegando **todo** cálculo en Quant Engine congelado. **Hallazgo previo al código**: la auditoría exigida antes de implementar encontró que `OptimizationProblem` tipaba su entrada como `bootstrap_sample: RValue[]` citando 02 §5.2 — la forma *informal anterior* a que 28 §6.1 la sustituyera por `Trade Set` (colección de Operaciones). Esa proyección con pérdida descartaba `r_final`, `risk_amount` y `closed_at`, haciendo incomputables dos de las cinco métricas de §4.2 y la comparación contra baseline que §10.2 declara obligatoria. **La información nunca faltó en el proyecto**: SPEC-005 era el único consumidor (de 006/007/009/011/012/013) que no usaba `Trade Set`. Corregido con diff mínimo, sin tocar arquitectura, algoritmos, invariantes ni interfaces. Implementa el ciclo `ask/tell` (§3.2) con `bounded_random_search.v1` y su garantía de cobertura `K ≥ ln(ε)/ln(1−q)`, independiente del tamaño del espacio (§2) — la respuesta formal a por qué la fuerza bruta sobre ≈2,3×10¹¹ combinaciones queda descartada. Motor puro: sin estado, sin persistencia, sin red, sin reloj, sin `Math.random()` — todo verificado por grep, no por intención; `evaluator-bridge`/`kernel` son el único punto que importa Quant Engine como valor (§3.3). Una estrategia **nunca ve un `QuantResult`**: solo números opacos ya evaluados, así que el algoritmo de búsqueda desconoce por completo cómo se calcula R. **Validación real**: golden test contra los números exactos de 12 §6 (muestra de `R_final` y `E[R]=1.2000`) — si el predicado de disparo de parciales fuera otro, no saldrían. 21 tests (golden, property test de 2.000 candidatas, reproducibilidad por semilla, independencia del orden de entrada, restricciones duras, Pareto acotado, explicabilidad). `tsc`/`eslint`/`npm test` limpios (186 tests en el monorepo). |
| [**ADR 0001 — Separación Core Domain vs. Plataforma SaaS**](./decisions/0001-separacion-core-domain-vs-plataforma-saas.md) | Decisión arquitectónica (estado: *Accepted*), sin código: el dominio de trading no puede conocer planes, límites, facturación ni estado de suscripción. Ninguna fórmula, ningún motor y ninguna tabla del dominio cambia de comportamiento según el plan contratado; la plataforma SaaS se sitúa en una capa exterior que puede *denegar la entrada*, nunca *alterar el cálculo*. Fija la frontera antes de que exista la primera línea de código de facturación, que es el único momento en que fijarla resulta barato. |
| **BUILD 006A — Event Backbone** (`supabase/migrations/20260803120600_event_backbone.sql`, `supabase/tests/04_event_backbone.sql`) | Build de infraestructura, no de producto: la auditoría previa a Rule Engine encontró **dos bloqueos reales**, verificados por grep y no inferidos del texto. (1) Operations Engine no emitía ninguno de los cinco eventos que SPEC-002 §3.1 le exige — el único escritor del outbox era Risk Engine, así que el pipeline entero de Rule Engine habría quedado inerte; deuda técnica introducida por BUILD 004. (2) No existía la secuencia monotónica de la que depende el hallazgo principal de SPEC-004 §14.2: `domain_events` solo tenía `id uuid` (aleatorio) y `occurred_at default now()`, que en Postgres es el instante de **inicio de transacción** — dos eventos de la misma transacción reciben timestamps idénticos. Era el Riesgo #1 de SPEC-004 materializado. Los eventos se emiten mediante triggers `AFTER` sobre las tablas fuente de verdad, nunca desde las RPC: cierra "path drift" (SPEC-002 §5.7) por construcción — cualquier vía futura (Trade Capture Engine, importador, corrección manual) emite sin poder olvidarlo — y da atomicidad gratuita. **Distinción central, la trampa clásica del patrón Outbox**: `event_sequence` (`generated always as identity`) **ordena**, `published = false` **entrega**; la secuencia da orden total pero no sirve como cursor (dos transacciones pueden tomar 5 y 6 y confirmarse al revés, y un lector con cursor perdería el 5 para siempre), ni es libre de huecos (un `ROLLBACK` consume número). Seis eventos con payload mínimo: identidad y la **forma de la transición**, nunca una copia del estado de otro módulo — el consumidor relee la fuente de verdad, que es lo que impide que el outbox se convierta en una segunda base de datos. 15 comprobaciones SQL nuevas contra Postgres real; las tres suites previas se reejecutaron completas, cero regresiones. |
| **BUILD 006B — Rule Engine** (`packages/rule-engine`, `supabase/migrations/20260803120700_rule_engine.sql`, `supabase/migrations/20260803120800_funding_compliance_cache.sql`, `supabase/tests/05_rule_engine.sql`) | Primer consumidor del Event Backbone, y **exclusivamente un motor de evaluación: nunca un motor de ejecución**. Produce veredictos con su evidencia y emite un evento; no cierra Operaciones, no bloquea al trader, no mueve capital y no escribe en ninguna entidad ajena — I17 ("Evaluar ≠ Actuar") aplicado al código, no solo al documento. **Dos contradicciones detectadas en la auditoría previa y resueltas por el fundador antes de escribir código**: (1) SPEC-004 se contradecía a sí misma sobre `compliance_flag` (su §1.2.3 dice que Rule Engine no modifica el estado de la Cuenta; su §4.3 paso 7 le ordena actualizarlo) — SPEC-003 §8.1 resuelve la autoridad: `accounts` pertenece a Funding Management, y **la actualización de un caché derivado corresponde siempre al módulo propietario**, principio que pasa a ser permanente; (2) la categoría `calculation` abría una puerta a que Rule Engine calculara, violando "Calcular ≠ Juzgar" — queda como categoría *reservada*, sin evaluador, sin definición y sin ningún camino de ejecución que pueda alcanzarla. La frontera de propiedad es visible en la estructura del repo, no solo en un comentario: son **dos migraciones separadas**, y es un trigger de Funding Management quien mantiene `compliance_flag` con *compare-and-set* sobre `compliance_flag_event_sequence`, de modo que un evento tardío (secuencia 5 tras la 10) no puede pisar un estado más reciente. `RuleEngineService.evaluar` implementa los pasos 1-6 y 8 de §4.3 y **omite explícitamente el paso 7**, con el motivo escrito en el código. Seis evaluadores puros (sin reloj, sin aleatoriedad, sin E/S), evaluación contra un Snapshot inmutable, `rule_evaluations` append-only por trigger, idempotencia por `(event_id, account_id)` comprobada dentro de la misma transacción, orden topológico con detección de ciclos, modo `shadow` para estrenar una regla sin que afecte a nadie, y veredicto `unavailable` —nunca cumplimiento asumido— cuando falta un dato. **Desacoplado de Quant Engine**: el paquete declara una única dependencia (`@tradepilot/risk-engine`) y no lo importa en ninguna línea. 29 tests unitarios + 16 comprobaciones SQL contra Postgres real, con la inmutabilidad verificada en **dos capas** (RLS como `authenticated`, y el trigger tras `reset role`) — una sola capa habría dado falsa seguridad frente a un `service_role`. `tsc`/`eslint`/`npm test` limpios (215 tests en el monorepo). |

## Nota sobre este repositorio

Este proyecto convive con la aplicación de fitness/nutrición existente en la raíz del repo. Es un producto independiente, con su propio stack, base de datos y ciclo de vida — la carpeta `tradepilot-r/` no depende de ningún código de la app de fitness ni lo modifica. Cuando el proyecto avance a fase de implementación (post-blueprint), se evaluará si conviene un repositorio dedicado o mantener el monorepo.
